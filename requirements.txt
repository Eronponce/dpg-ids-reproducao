pandas>=2.0
numpy>=1.24
scikit-learn>=1.5
scipy>=1.10
networkx>=3.0

# o comando 6 reconstroi o grafo supervisionado. A versao e a declarada no
# capitulo, e nao a mais recente: a 0.2.0 existe e muda a saida.
dpg==0.1.6

# a DPG-iForest esta embarcada em vendor/dpg_iforest, porque nao e distribuida
# pelo PyPI. Estas sao as dependencias dela, usadas pelo comando 8.
joblib>=1.3
graphviz>=0.20
matplotlib>=3.7
seaborn>=0.13
portion>=2.4
